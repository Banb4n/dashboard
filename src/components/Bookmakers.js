/* @flow */
import React from 'react';

function Bookmakers(props: { user: Object }) {
    // const { user } = props;

    return (
        <div className="bookmakers">
            <h1>Bookmakers</h1>
        </div>
    );
}

// const UserProfileConnected = (props: any) => (
//     <Subscribe to={[AppContainer]}>
//         {appContainer => (
//             <UserProfile user={appContainer.state.user} {...props} />
//         )}
//     </Subscribe>
// );

export default Bookmakers;
