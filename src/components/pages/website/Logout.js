/* @flow */
import * as React from 'react';
import { StyleSheet } from 'aphrodite-jss';

const STYLES = StyleSheet.create({});

function Logout(appProps: {}): React.Node {
    return (
        <div className="HomePage">
            <h1>HomePage</h1>
        </div>
    );
}

export default Logout;
